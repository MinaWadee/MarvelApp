<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.extremesolution.marvelapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.MarvelApp"
        tools:replace="android:allowBackup"
        android:launchMode="singleTop"
        android:usesCleartextTraffic="true"
        android:allowClearUserData="true"
        android:hardwareAccelerated="true">

        <activity android:name=".appActivities.SplashActivity"
            android:theme="@style/Theme.SplashMarvelApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".appActivities.MainActivity" />

    </application>

</manifest>